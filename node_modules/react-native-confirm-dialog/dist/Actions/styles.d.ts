import { Theme } from '../types';
declare const getStyles: ({ spacing }: Theme) => {
    actions: {
        marginTop: number;
        flexDirection: "row";
        justifyContent: "space-between";
    };
    cancel: {
        borderColor: string;
        flex: number;
        marginRight: number;
    };
    confirm: {
        flex: number;
    };
    blackLabel: {
        color: string;
    };
};
export default getStyles;
