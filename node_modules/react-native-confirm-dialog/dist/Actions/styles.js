import { StyleSheet } from 'react-native';
const getStyles = ({ spacing }) => StyleSheet.create({
    actions: {
        marginTop: spacing * 2,
        flexDirection: 'row',
        justifyContent: 'space-between'
    },
    cancel: {
        borderColor: 'black',
        flex: 1,
        marginRight: spacing,
    },
    confirm: {
        flex: 2,
    },
    blackLabel: {
        color: 'black',
    },
});
export default getStyles;
