/// <reference types="react" />
import { ConfirmConfig } from './types';
interface Props {
    children: JSX.Element;
    config?: Partial<ConfirmConfig>;
}
declare const ConfirmProvider: ({ children, config }: Props) => JSX.Element;
export default ConfirmProvider;
