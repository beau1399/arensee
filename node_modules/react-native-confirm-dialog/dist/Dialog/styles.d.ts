import { Theme } from '../types';
declare const getStyles: ({ spacing, backshadowColor }: Theme) => {
    centeredView: {
        flex: number;
        justifyContent: "center";
        alignItems: "center";
        backgroundColor: string;
    };
    modalView: {
        backgroundColor: string;
        borderRadius: number;
        paddingHorizontal: number;
        paddingVertical: number;
        width: string;
    };
    title: {
        fontSize: number;
    };
    subtitle: {
        marginTop: number;
        color: string;
        fontWeight: "400";
        fontStyle: "italic";
    };
    body: {
        paddingVertical: number;
        fontSize: number;
        marginBottom: number;
    };
};
export default getStyles;
