import { Theme } from '../types';
declare const getStyles: (theme: Theme) => {
    button: {
        backgroundColor: string;
        justifyContent: "center";
        alignItems: "center";
        borderRadius: number;
        flexDirection: "row";
    };
    inverse: {
        backgroundColor: string;
        borderColor: string;
        borderWidth: number;
    };
    loadingIndicator: {
        marginRight: number;
    };
    label: {
        paddingVertical: number;
        color: string;
    };
    inverseLabel: {
        color: string;
    };
};
export default getStyles;
