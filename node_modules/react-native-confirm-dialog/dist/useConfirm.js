var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import { useContext } from 'react';
import { ConfirmContext } from './context';
const useConfirm = () => {
    const _a = useContext(ConfirmContext), { setCurrent } = _a, current = __rest(_a, ["setCurrent"]);
    return (config) => {
        setCurrent &&
            setCurrent(Object.assign(Object.assign(Object.assign({}, current), config), { open: true, theme: Object.assign(Object.assign({}, current.theme), config === null || config === void 0 ? void 0 : config.theme) }));
    };
};
export default useConfirm;
